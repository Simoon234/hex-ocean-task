import { DishesFields } from "../types";

const initialFormikValues: DishesFields = {
  diameter: 0,
  name: "",
  no_of_slices: 0,
  preparation_time: "",
  slices_of_bread: 0,
  spiciness_scale: 0,
  type: "",
};

export default initialFormikValues;
